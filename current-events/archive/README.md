# Archived, superseded 2026-07-29

## Event 01, The Rise of Social Media

Replaced by Lesson 1, `lesson-01/`, "Reverse History: The Ban in Your Locker".

Both traced phones and platforms backward, so keeping the two side by side meant
two adjacent phone traces on two different architectures. The cell phone ban
lesson supersedes it.

| File | Was |
|---|---|
| `archive/event-01-social-media-index.html` | `event-01/index.html` |
| `archive/ce-event-01-social-media.js` | `assets/data/ce-event-01-social-media.js` |
| `archive/behistorical-ce-event-renderer.js` | `assets/js/behistorical-ce-event-renderer.js` |

The renderer is archived with the event because it implemented the five-step
rhythm (Brief, Where in the World, Trace, Evidence Lab, Your Beat Checkpoint).
Lesson pages now follow the AP World lesson architecture instead: a thin shell
plus a data file plus a renderer-config file plus
`assets/js/behistorical-ce-lesson-renderer.js`.

Nothing here is referenced by a live page. `behistorical-ce-event.css` was NOT
archived, because `your-beat/`, `culture/` and `teacher/` still use it.

The 1833 penny-press trace in the archived data file is still good material if
you ever want it as its own event.
